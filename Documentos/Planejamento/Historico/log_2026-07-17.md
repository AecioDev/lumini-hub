# Log - 2026-07-17

## Atividades Realizadas

- Lido e resumido o histórico de planejamento anterior (`Documentos/Lumini Hub Project Initiation.md`, sessão feita no Antigravity IDE) sobre o desenho do `api.integrations`.
- Atualizado `CLAUDE.md` com o escopo do `api.integrations` (middleware bidirecional Loja Integrada ↔ SQL Server legado) e a decisão de UI híbrida (Ant Design + Tailwind/rizzui) para o módulo CRM.
- Validada com o usuário a decisão de manter a Loja Integrada como loja virtual (descartada a ideia de construir uma loja própria).
- Planejado e implementado a **Fase 1 do `api.integrations`** (porta 4007) — estrutura, conexões e gateway, sem a lógica de negócio que depende de pendências:
  - Novo módulo Go `api.integrations`, registrado em `go.work`.
  - Domain + `AutoMigrate` das tabelas próprias no Postgres: `IntegrationConfig`, `SyncLog`, `ProductMapping`, `WebhookEvent`.
  - Repositório + `UnitOfWork` Postgres seguindo o padrão do `api.core`.
  - Camada de leitura do SQL Server legado (sem UoW/AutoMigrate): `LogAltera` (polling), `CadSeq`/`GetSequencia` (com teste, skip-if-unconfigured), `LocArm`, `Cencus`.
  - `ConfigService` com cache em memória para as chaves da Loja Integrada e códigos do ERP legado (`li_api_key`, `li_app_key`, `li_webhook_secret`, `codtipnot`, `codlocarm_oficial`, `codlocarm_reserva`, `codemp`).
  - Handlers + rotas: `/settings` (GET/PUT), `/sync-logs/filter`, `/webhook-events/filter`, `/legacy/locations`, `/legacy/companies`, e o receptor público `/webhooks/loja-integrada` (validado por segredo compartilhado, sem `AuthMiddleware`).
  - Proxy no `api.gateway`, entrada no `run_services.bat`, Swagger regenerado.
  - Frontend: tela **Configurações → Integrações** (formulário de configuração + selects de local/empresa alimentados pelo legado + tabelas de sync logs e webhook events), com services e schemas Zod correspondentes.
  - `go build`/`go test` e `tsc --noEmit` executados sem novos erros.
- Corrigido durante a implementação: o arquivo `loc_arm.go` foi renomeado para `loc_arm_model.go`, pois o Go trata nomes de arquivo terminados em `_arm.go` como build constraint da arquitetura ARM e os ignora silenciosamente ao compilar para `amd64`.
- Criado `Documentos/Planejamento/Modulo_1_CRM_Integracoes/tasks_integrations.md`.

## Gargalos / Pendências

- **Passo manual pendente:** criar as permissões `integrations.view`/`integrations.edit` via `POST /api/permissions` (não foi automatizado no código porque `permissions` é tabela do `api.auth`, e serviços não devem escrever direto na tabela um do outro).
- **Fase 2 do `api.integrations`** (lógica real de sincronização) segue bloqueada por:
  - Chaves de API/App da Loja Integrada (usuário estava obtendo acesso).
  - Valor de `codtipnot` a ser usado nos pedidos vindos da LI.
  - Política de prioridade de estoque entre loja física e virtual — usuário ia confirmar com a diretoria do cliente.
- O `go build ./...`/`go test ./...` a partir da raiz de `Backend/` falha por causa de um `go.mod` legado solto em `Backend/go.mod` (módulo `simple-erp-service`, não listado em `go.work`) — pré-existente, não relacionado a esta sessão. Build/test funcionam normalmente listando os módulos explicitamente.
- CLAUDE.md ainda descreve a stack de UI do frontend como "rizzui/Radix UI", mas o código real usa shadcn/ui — discrepância identificada durante a exploração, ainda não corrigida no CLAUDE.md (fora do escopo desta sessão).
