# Log - 2026-07-19

## Atividades Realizadas

- **Frontend reconstruído do zero** (Next.js + Tailwind + `rizzui`/Radix → **Vite + React 18 + TypeScript + Ant Design v5 + React Router v6**), a pedido do usuário, usando como referência visual `Documentos/exemplos/design_handoff_erp_frontend/` (README + `ERP.dc.html` + screenshots — telas de Login, Dashboard CRM, Usuários, Novo Usuário). Todo o conteúdo antigo de `Frontend/` já estava apagado no working tree (deleção não commitada) antes de começar.
- Mapeado o contrato real da API lendo os handlers/domain Go diretamente (`swagger.json` estava desatualizado — faltavam roles/permissions/menu-items) para não inventar contrato: rotas do gateway por prefixo (`/api/auth`, `/api/users`, `/api/roles`, `/api/permissions`, `/api/menu-items` → api.auth; `/api/customers`, `/api/suppliers` → api.core), formato de resposta padrão, paginação, DTOs de Usuário/Role/Permission/MenuItem/Customer/Supplier.
- Montado o novo projeto: `package.json`/`vite.config.ts` (porta fixa 3000, exigida pelo CORS do gateway) /`tsconfig`/`eslint.config.js`, camada `types/` + `services/` por entidade (axios único com interceptor de refresh-token em 401), `schemas/` Zod por formulário, `contexts/` (`AuthContext` com bootstrap via `GET /auth/me`, `ThemeContext` dark/light), `routes/` (`router.tsx`, `ProtectedRoute`, `RequirePermission`, `AuthLayout`, `AppLayout` com Sider colapsável 240/76px), `pages/` e `components/` por domínio.
- Implementado: Login (campo `username` real, não e-mail — contrato exige) e Recuperar Senha (UI completa, mas sem endpoint no backend — mostra aviso em vez de fingir sucesso); Dashboard CRM (mock isolado em `services/crm/crm-dashboard-service.ts`, já que `api.crm` — porta 4009 — ainda é só planejado); módulo Usuários completo (listagem paginada via `GET /users`, criação/edição com preset de Perfil + matriz de permissões, `PUT /users/:id/permissions` só quando customizado).
- **Sem tela de Cadastro/Signup** — decisão do usuário: é um ERP, contas são criadas pelo admin via módulo Usuários, não há autocadastro público.
- Testado com a API real rodando (`admin`/`987321`) e corrigidos, a partir do feedback do usuário com prints de tela/console:
  - CSS de reset ausente causando borda branca ao redor da tela (`src/index.css`, faltava desde o scaffold inicial).
  - Bug real na extração de colunas da matriz de permissões: código de permissão nem sempre é `modulo.acao` de 2 segmentos (ex.: `dashboard.admin.view`, 3 segmentos) — corrigido extraindo pelo último `.` e detectando colisão.
  - Menu lateral com `key` duplicada no antd `Menu` (hrefs repetidos em pontos diferentes da árvore real, ex. "Clientes" em Vendas>Cadastros e em Financeiro>Contas a Receber) — key trocada de `href` para `id`.
  - Warnings de "Future Flag" do React Router 6→7 silenciados via `future` flags.
  - Interceptor do axios forçava reload duro (`window.location.href`) mesmo na checagem silenciosa de sessão (`GET /auth/me`) — ajustado para deixar o `AuthContext`/`ProtectedRoute` tratar esse caso via SPA, mantendo o redirect duro só pra sessão que expira durante o uso.
  - Rota de Usuários corrigida de `/users` para `/settings/users` (href real semeado em `menu_items`, diferente do que eu tinha assumido).
- Redesenhada a UI de permissões por pedido do usuário, em duas iterações: (1) trocado o hover "grade Módulo x Ação" original por um seletor de módulo dedicado — rejeitado pelo usuário; (2) reaproveitados os próprios botões de Perfil (presets de Role, dinâmicos via `GET /roles`) para também controlar qual módulo aparece à direita (por nome, com fallback pro primeiro módulo com permissão marcada quando o nome do Perfil não bate com nenhum módulo — ex. "Gerente"/"ADMIN"); (3) permissões "extras" (que não são Ver/Criar/Editar/Excluir) deixaram de ter seção separada, viraram uma lista única lado a lado com `flex-wrap`; (4) formulário de Novo/Editar Usuário separado em abas antd (`Tabs`) "Dados do Usuário" / "Permissões", com o card de dados ocupando a largura toda em grid responsivo; (5) adicionado hover nos itens do menu lateral via tokens do tema (`components.Menu.itemHoverBg`).
- Corrigido de memória (sessão anterior, mesmo bug reaparecendo no front novo): `GET /roles` não garante ordem estável no backend — `role-service.ts` agora ordena por nome no client antes de usar `roles[0]` como preset padrão.
- `CLAUDE.md` atualizado: seção "Frontend Architecture" reescrita para o stack novo (estrutura de pastas, padrão de CRUD com abas, gotcha do `key` do menu), comandos (`VITE_API_URL`, `eslint .`), overview do projeto.
- Memória (`~/.claude/projects/.../memory/`) limpa e atualizada: removidas duas memórias específicas do frontend antigo que ficaram obsoletas (bug de scroll do Radix Checkbox, rework de Usuários/Perfis em Next.js), atualizado o file path na memória do menu dinâmico, criadas memórias novas do rebuild e das preferências de UX do usuário (reaproveitar elementos existentes em vez de criar novos, listas unificadas sem sub-seção visual, forms full-width, abas para formulários complexos).
- **Frontend de Integrações reconstruído** em `/settings/integrations` (o backend do `api.integrations`, Fase 1, já estava pronto desde `f3bf706` — só o consumidor Next.js tinha sido apagado no rebuild): abas Visão Geral (stats reais via `page_size=1` nos endpoints de filtro, sem endpoint de estatística dedicado), Configurações (chaves da Loja Integrada + selects de local/empresa do ERP legado, com fallback gracioso se o SQL Server legado estiver indisponível) e Logs (Sync Logs + Webhook Events paginados/filtráveis).
- **Telas de Perfis e Permissões** construídas em `/settings/roles` (mesma rota do item de menu real "Perfis e Permissões", reaproveitado em vez de criar entrada nova) — CRUD completo de Roles e Permissions, com um editor de permissões por Role reaproveitando o `ModulePermissionsPanel` dos Usuários (mas com lista de módulos própria, já que aqui não tem "preset" pra clicar).
- **RBAC virou hierárquico em 2 níveis**, a pedido do usuário: a tela de Perfis e Permissões não deve ficar disponível nem pro ADMIN (criar Role/Permission sem o `RequirePermission(...)` correspondente já existir no código não tem efeito nenhum — é ferramenta de desenvolvedor, não de administrador do negócio). Criado o perfil `DEVELOP` (só no código por enquanto, sem seeder — o usuário vai cadastrar a Role e o usuário manualmente no banco) com bypass total real, e o `ADMIN` passou a ter bypass em tudo **exceto** `admin.create_permissions`/`roles.*`/`permissions.*`. Implementado nos dois lados: backend (`common/utils/rbac.go` novo, `middlewares.RequirePermission`, `domain.FilterMenuTreeForUser`) e frontend (`AuthContext.tsx`'s `hasPermission`), pra tela e API ficarem consistentes. Testado ponta a ponta via curl: `GET /api/roles` como ADMIN agora retorna `403`, item "Perfis e Permissões" sumiu do `menu_items` do login, `GET /api/users` continua `200` (resto do acesso do ADMIN intacto). Também precisou limpar dado já existente: removidas `admin.create_permissions`/`roles.*`/`permissions.*` do `role_permissions` da Role ADMIN e do `user_permissions` do usuário `admin` (id=2) via `PUT /roles/1/permissions` e `PUT /users/2/permissions` — sem isso a mudança de código sozinha não bastava, porque esse usuário já tinha essas permissões diretamente concedidas.

## Gargalos / Pendências

- **Recuperar senha** não tem endpoint no backend (só `/auth/login|refresh-token|logout|me`) — tela pronta, aguardando a API.
- **Dashboard CRM inteiro é mock** — depende do `api.crm` (porta 4009), ainda não implementado (ver `Modulo_1_CRM_Integracoes/plano_crm.md`).
- **Busca de usuários é client-side** (só filtra a página carregada) — `GET /users` não tem parâmetro de busca no backend.
- **Catálogo de Perfis/Permissões com nomenclatura inconsistente**, confirmado ao vivo com o usuário: nomes de Role (`Gerente`, `ADMIN`) não batem com nome de módulo nenhum, e alguns códigos de permissão têm 3 segmentos (`dashboard.admin.view`) em vez do padrão `modulo.acao`. O frontend já tem fallback pra isso, mas o usuário pretende reorganizar isso no banco — vale revisitar a UI de permissões quando isso acontecer.
- **Usuários, Integrações e Perfis/Permissões** têm CRUD real no frontend novo hoje; todo o resto do menu (Vendas, Compras, Estoque, Financeiro) ainda cai no `PlaceholderPage`.
- Bundle de produção acima de 500kB (antd é pesado) — sem code-splitting ainda; não é bloqueante agora, mas vale revisitar com `manualChunks`/`dynamic import` se a lentidão de load incomodar.
- **Falta criar a Role `DEVELOP` e o usuário desenvolvedor** — decisão do usuário fazer isso manualmente direto no banco, não via seeder/API. Até isso existir, ninguém acessa `/settings/roles` (nem o ADMIN, que perdeu o bypass de propósito).
- Commit desta sessão ainda pendente (aguardando o usuário pedir).
