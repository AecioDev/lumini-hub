# Log - 2026-07-18

## Atividades Realizadas

- Investigado o relato do usuário de que a tela de Perfis não estava funcionando: confirmado que `/settings/roles` (componentes `RoleList`/`RolePermissionsTable`) chamava `POST /roles/:id/permissions/link` e `DELETE /roles/:id/permissions/unlink/:id`, rotas inexistentes no backend — resquício órfão de antes do refactor `6919a94`, que introduziu um fluxo novo (`CreateRoleDialog`/`EditRoleDialog`) sem nunca atualizar essa tela.
- A pedido do usuário, reorganizadas as telas de Usuários/Perfis/Permissões:
  - **`/settings/users`** ("Usuários"): simplificada — removido o grid de cards de "Perfis de Acesso" e o botão "Novo Perfil"; ficou só a criação/edição de usuário (com seleção de Perfil + personalização de permissões individuais) e a tabela de usuários. Corrigido bug de copy-paste no guard (`customers.view` → `users.view`).
  - **`/settings/roles`** ("Perfis e Permissões"): reescrita como ferramenta **dev-only** (gate `admin.create_permissions`) — select de Perfil + criar/editar/excluir perfil + lista de permissões do perfil selecionado (filtrada por módulo) com botão "Salvar Alterações".
  - Removida a entrada de menu "Permissões" (apontava para `/settings/permissions`, rota vazia/morta) e a pasta correspondente.
- Extraído componente compartilhado `PermissionsPicker` (`src/components/settings/permissions/permissions-picker.tsx`), consolidando lógica que estava duplicada 4x (`add-user-dialog`, `edit-user-dialog`, `create-role-dialog`, `edit-role-dialog`), com melhor UX de scroll/agrupamento por módulo.
- Confirmado, investigando o código real (não a documentação), que o backend **já implementa** o modelo de RBAC híbrido que o usuário descreveu: `Role` é só template/default; o controle de acesso efetivo é via `user_permissions` (vínculo direto usuário↔permissão), montado no JWT no login. `CLAUDE.md` estava desatualizado nesse ponto — corrigido.
- Corrigido backend (`api.auth/internal/routes/routes.go`): rotas de `/roles` agora exigem `roles.view/create/edit/delete` em vez de `users.*` (o catálogo já tinha essas permissões cadastradas, sem uso).
- Corrigido bug pré-existente em `role-service.ts`: `createRole`/`updateRole` liam `response.data.id` em vez de `response.data.data` (formato padrão do `utils.SuccessResponse`), retornando sempre um objeto com campos `undefined`. Só não tinha sido notado porque os chamadores anteriores ignoravam o retorno.
- Corrigido `POST /users` exigir `email` no backend sem o formulário marcar o campo como obrigatório (`add-user-dialog.tsx`, depois migrado — ver abaixo).
- **Novo padrão de CRUD do sistema, a pedido do usuário**: páginas completas de Cadastro/Edição (react-hook-form + Zod) em vez de modais, mantendo modal só para confirmação de ações destrutivas (`ConfirmDeleteDialog`) e toasts para feedback — documentado no `CLAUDE.md` (seção "Mandatory frontend CRUD pattern"). Migradas as duas únicas entidades com CRUD real hoje:
  - Usuários: `add-user-dialog`/`edit-user-dialog` → `src/components/user/forms/create-user-form.tsx`/`edit-user-form.tsx` + rotas `settings/users/create` e `settings/users/[id]/edit` (antes stubs `em-construcao`).
  - Perfis: `create-role-dialog`/`edit-role-name-dialog` → `src/components/settings/roles/forms/create-role-form.tsx`/`edit-role-form.tsx` + novas rotas `settings/roles/create` e `settings/roles/[id]/edit` (`routes.ts` `settings.roles` passou de string para objeto `{root, create, edit}`). A edição de permissões do perfil continua inline na tela principal (não é modal, não precisou migrar).
  - Extraído `src/components/common/form-field.tsx` (wrapper label+erro), replicando o padrão já usado em `basic-info-customer-form.tsx`.
  - Todos os dialogs antigos substituídos foram apagados, incluindo o `edit-role-dialog.tsx` que já estava órfão.
  - Achado no meio do caminho: `GET /roles` não tem ordenação estável no backend, o que fazia a tela de Perfis selecionar um perfil diferente por padrão a cada carregamento — contornado ordenando por nome no frontend (não mexeu no backend).
- Investigado o restante do sistema para avaliar o mesmo tipo de migração: quase nenhuma outra entidade (Produtos, Fornecedores, Pedidos de Venda/Compra, Endereços, Contatos, Documentos, Financeiro AP/AR) tem CRUD real hoje — são rotas `em-construcao`, algumas sem nem service implementado, e o próprio módulo de referência (Clientes) só tem a criação pronta (edição/visualização também stub). Decisão do usuário: aplicar o padrão "conforme for mexendo em cada entidade", não migrar tudo de uma vez.

## Gargalos / Pendências

- Segue valendo o que já estava registrado para o `api.integrations` (Fase 2 bloqueada por chaves da Loja Integrada, `codtipnot` e política de estoque).
- Próximo passo combinado com o usuário: discutir o Sidebar/menu de navegação (relacionado a acessos/permissões).
- Fora do escopo desta sessão, identificado mas não corrigido: DTOs de Fornecedores desalinhados com a API real; descrições erradas (copy-paste de "Usuários") nos seeds de permissões do módulo `roles`.
